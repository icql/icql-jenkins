# service
---
kind: Service
apiVersion: v1
metadata:
  name: svc-template
  namespace: 10_namespace-template
  labels:
    app: svc-template
spec:
  ports:
    - name: dashboard
      protocol: TCP
      port: 8200
      targetPort: 8200
  selector:
    app: pod-template